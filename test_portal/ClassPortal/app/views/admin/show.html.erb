<hi>Display All Users</hi>
<table>
  <thead>
  <tr>
    <th>  E-mail  </th>
    <th>  Name  </th>
    <th>  Is Admin??  </th>
    <th>  Is Instructor??  </th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
      <tr>
        <td><%= user.email %><br></td>
        <td><%= user.name %><br></td>
        <td><%= user.is_admin %><br></td>
        <td><%= user.is_instructor %><br></td>
        <td><%= link_to 'Edit',user, :controller => 'users', :action => 'edit'%></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<table>
  <thead>
  <tr>
    <th>  Course No.  </th>
    <th>  Title  </th>
    <th>  Description  </th>
    <th>  Materials  </th>
    <th>  Is Active??  </th>
    <th>  Start Date  </th>
    <th>  End Date  </th>
  </tr>
  </thead>

  <tbody>
  <% @courses.each do |course| %>
      <tr>
        <td><%= course.course_id %><br></td>
        <td><%= course.title %><br></td>
        <td><%= course.description %><br></td>
        <td><%= course.materials %><br></td>
        <td><%= course.is_active %><br></td>
        <td><%= course.start_date %><br></td>
        <td><%= course.end_date %><br></td>
        <td><%= link_to 'Edit',course, :controller => 'courses', :action => 'edit'%></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<%= link_to('Back', :controller => 'admin' , :action => 'index')%>