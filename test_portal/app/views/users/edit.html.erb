<h1>Editing User</h1>


<!%= render 'form' %>

<%= form_for(@user) do |f| %>
    <% @user_1 = User.find(params[:id])%>
    <% @user_2 = User.find(session[:id])%>
    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <!% @user_1 = User.find(params[:id])%>
      <!% @user_2 = User.find(session[:id])%>
      <% if @user_1 == @user_2 %><!%User.find_by_email(:email) === User.find_by_email(session[:email])%>
        <%= f.label :password %><br>
        <%= f.text_field :password %>
      <% end %>
      <!%= f.text_field User.find(session[:id]) %><br>
    </div>

    <% if @user_2.is_admin %>
    <div class="field">
      <%= f.label :is_admin %><br>
      <%= f.text_field :is_admin %>
    </div>

    <div class="field">
      <%= f.label :is_instructor %><br>
      <%= f.text_field :is_instructor %>
    </div>
    <%end %>



    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

<!%= link_to 'Show', @user %> |
<!%= link_to 'Back', users_path %>
<% @user_1 = User.find(session[:id]) %>
<% if @user_1.is_admin %>
    <%= link_to('Back', :controller => 'admin' , :action => 'index')%>
<% elsif @user_1.is_instructor%>
    <%= link_to('Back', :controller => 'instructor' , :action => 'index')%>
<%else %>
    <%= link_to('Back', :controller => 'student' , :action => 'index')%>
<%end%>
