<%= form_for(@portal_admin) do |f| %>
  <% if @portal_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portal_admin.errors.count, "error") %> prohibited this portal_admin from being saved:</h2>

      <ul>
      <% @portal_admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
